import SwiftUI

struct MovieDetailedView: View {
    var body: some View {
        Image("4")
            .resizable()
            .scaledToFit()
        
        Spacer()
        
        
    }
}

#Preview {
    MovieDetailedView()
        .preferredColorScheme(.dark)
}
